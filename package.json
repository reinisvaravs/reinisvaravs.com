{
  "name": "padlet-all",
  "version": "1.0.0",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "build": "npm run build:padlet && npm run build:portfolio",
    "build:padlet": "cd padlet-clone && npm install && npm run build",
    "build:portfolio": "cd portfolio && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.21.2",
    "cors": "^2.8.5",
    "serve-static": "^1.15.0"
  }
}
